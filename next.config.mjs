/** @type {import('next').NextConfig} */
const nextConfig = {

    images:{
        domains:["upload.wikimedia.org", "th.bing.com", "files.porsche.com", "encrypted-tbn0.gstatic.com", "cdn.motor1.com"] 
    }
};

export default nextConfig;
